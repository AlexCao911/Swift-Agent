#!/bin/sh
# LocalAgent URL interceptor — routes xdg-open(1) into the in-app browser
# preview (WebKit) via the OSC LocalAgentOpenURL marker. See localagent-open.
exec /usr/local/bin/localagent-open "$@"
